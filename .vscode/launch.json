// A launch configuration that compiles the extension and then opens it inside a new window
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Basic Client",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceRoot}/editors/code/basic",
        "--disable-extensions",
        "${workspaceRoot}/examples"
      ],
      "outFiles": ["${workspaceRoot}/client/dist/*.js"],
      "preLaunchTask": "Build Basic Server and Extension"
    }
  ]
}
